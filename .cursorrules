# Scratchpad

## Tarefa Concluída: NF Conjunta vs Separada (OF com consumo + permanente) (2026-03-01)

- [X] Ordem: modo_envio_nf (CONJUNTA | SEPARADA); NF: tipo_itens (CONSUMO | PERMANENTE)
- [X] Migration 1772700000000; API definir modo; upload com tipo_itens
- [X] Fornecedor: pergunta ao anexar NF; 2 slots quando separada
- [X] Órgão: mesma pergunta ao importar XML; 2 slots quando separada
- [X] 2 NFs → 2 recebimentos distintos; matching-ia e criarComNF filtram por tipo
- [X] Pré-análise: quando NF separada, filtrar itensOf por tipo_itens da NF — só valida itens da NF atual (ex: NF Permanente não flagra GRAMPEADOR)
- [X] Anexar 2ª NF após aceite da 1ª: Fornecedor mostra card NF em ATENDIDA_PARCIAL; Órgão mostra "Ainda falta anexar NF X" em EtapaNF + banner em mapeamento/recebimento

## Tarefa Concluída: REQ-ALM-001 - Evitar ateste duplicado em 2º recebimento (2026-03-01)

- [X] Permitir 2º XML em OF ATENDIDA_PARCIAL (não substituir NF existente)
- [X] Matching IA: enviar apenas itens pendentes (quantidade_entregue < quantidade)
- [X] criarComNF: validar e bloquear itens já totalmente recebidos
- [X] Frontend: alerta quando mapeamento contém itens já recebidos; itensPendentes no select
- [X] Fluxo múltiplas NFs: recAtivoParaNfAtual para exibir recebimento correto

## Tarefa Concluída: Dossiê do Fiscal (2026-03-01)

- [X] Documento "Comprovação de Aceite" (PDF assinado) ao concluir aceite
- [X] Página "Dossiê do Fiscal" /orgao/fiscal/dossie com OF + NF + Comprovação + anexos
- [X] Upload de anexos, download ZIP, marcar entregue ao financeiro
- [X] Notificação ao fiscal quando dossiê disponível
- [X] Fix: PDF não gerando — log de erro melhorado; endpoint POST recebimentos/:id/gerar-comprovacao-aceite para retry manual
- [X] Tela recebimento: card "Dossiê do Fiscal" quando aceite concluído — Baixar PDF / Gerar PDF + link "Deseja anexar mais algum documento?"

## Tarefa Concluída: Clareza OF + Botão único Enviar (2026-03-01)

- [X] Requisições: banner no modal detalhes quando OF gerada — "Ordem de Fornecimento gerada", link para Ordens de Fornecimento
- [X] Requisições: badge na tabela "OF gerada — acesse Ordens de Fornecimento" para não-OS com ordem_fornecimento_id
- [X] Ordens: um único botão Enviar/Reenviar ao fornecedor (PDF assinado)
- [X] Modal unificado: email e telefone editáveis, opções Enviar por Email e Enviar por WhatsApp
- [X] Backend enviarAoFornecedor já atualiza status para ENVIADA quando EMITIDA

## Tarefa Concluída: Inconsistência quantidade_entregue após exclusão de requisições (2026-03-01)

- [X] Problema: Ao excluir requisições, recebimentos zerados mas itens_contrato.quantidade_entregue permanecia (ex: Grampeador 5)
- [X] Causa: Recebimentos removidos sem passar pelo fluxo de estorno (ex: CASCADE, SQL direto) ou bugs em cancelarOrdem/estornar
- [X] RecebimentoService.estornar: aceitar ACEITO_PARCIAL (antes só ACEITO)
- [X] OrdemFornecimentoService.cancelarOrdem: incluir ACEITO_PARCIAL na busca de recebimentos a reverter
- [X] RequisicaoService.excluir: safeguard recebimento.itens || [] e quantidade_aceita > 0
- [X] Migration 1772200000000-ReparoQuantidadeEntregueItensContrato: recalcula quantidade_entregue a partir dos recebimentos existentes; se tabela recebimentos vazia, zera
- [X] Fix: app.module migrations path dist/migrations → dist/src/migrations (NestJS compila para dist/src/)
- [X] Script scripts/reparo-quantidade-entregue.ts + npm run reparo:quantidade-entregue para reparo manual

## Tarefa Concluída: Assinatura digital e envio ao fornecedor para OF (2026-03-01)

- [X] EntidadeTipo.ORDEM_FORNECIMENTO, TipoNotificacao.ORDEM_FORNECIMENTO_APROVADA
- [X] OrdemFornecimento.codigo_validacao + migration
- [X] gerarPdfOrdemFornecimento com assinaturas opcionais (quadro assinaturas eletrônicas)
- [X] Fluxo autorizar: assinatura + PDF assinado + notificarFornecedorOF (email, WhatsApp, notificação)
- [X] Checkbox enviar_ao_fornecedor e envio_automatico_os no modal autorizar (reutilizado para OF)
- [X] POST ordens/:id/enviar-ao-fornecedor para reenvio
- [X] enviarOrdem/reenviarOrdem usam caminho_pdf (assinado) quando disponível

## Tarefa Concluída: PDF Ordem de Fornecimento no modelo da Ordem de Serviço (2026-03-01)

- [X] GeradorPdfService.gerarPdfOrdemFornecimento: mesmo layout visual da OS
- [X] Cabeçalho: logo + dados do órgão, título OF/OS, número
- [X] Campos informativos: Número, Data Emissão, Órgão, Contrato, Fornecedor, Setor, Local, Solicitante
- [X] Objeto/Justificativa, tabela de itens (Descrição, Unidade, Qtd., Valor Unit., Total)
- [X] Rodapé: texto sobre NF + espaço para assinatura
- [X] PdfOrdemService delegando para GeradorPdfService

## Tarefa Concluída: Cancelar recebimento com aceite parcial - restaurar empenhado (2026-03-01)

- [X] Ao estornar/cancelar recebimento que teve aceite parcial: restaurar quantidade_empenhada
- [X] Fluxo: aceite reduz empenhado e aumenta entregue; cancelar deve reverter AMBOS (entregue-=, empenhado+=)
- [X] Motivo: ordem ainda existe (ENVIADA), unidades continuam reservadas para reenvio do fornecedor

## Tarefa Concluída: Cancelar Recebimento com Aceite Parcial (2026-02-17)

- [X] Ao cancelar recebimento que teve aceite parcial (almoxarifado ou patrimônio), reverter baixa no contrato e zerar aceites
- [X] Limpar aceite_almoxarifado_*, aceite_patrimonio_*, valor_aceito, quantidade_aceita
- [X] Chamar reverterAtendimento para devolver quantidade_entregue na OF
- [X] Assim a substituição de NF passa na validação (temAceite = false)

## Tarefa Concluída: Aceite Separado Almoxarifado/Patrimônio (2026-02-17)

- [X] Backend: aceite_almoxarifado_*, aceite_patrimonio_* na entity Recebimento
- [X] Backend: aceitarAlmoxarifado, aceitarPatrimonio, calcularStatus, estornar com motivo_estorno
- [X] Backend: compatibilidade recebimentos antigos em calcularStatus
- [X] Frontend: botões Warehouse (almox) e Archive (patrimônio), modais separados
- [X] Frontend: fallback botão Aceitar para recebimentos legados sem tipo_item
- [X] Migration 1772100004000-AddAceiteSeparadoRecebimento

## Tarefa Concluída: OS com Medição por Etapas (2026-02-17)

- [X] Backend: RequisicaoEtapaOS, DTO etapas_os, validação Global vs Demanda, saldo por etapa
- [X] Backend: atualizar() com itens_os/etapas_os (edição em rascunho)
- [X] Frontend: UI etapas + Ordem Global/Demanda, objeto espelho do contrato
- [X] Frontend: Edição de OS em rascunho (link Editar → /nova?editar=id, carrega itens/etapas)
- [X] Frontend: Cancelar/Excluir já visíveis para OS (não restritos por tipo)

## Tarefa Concluída: Requisições e OS - Tela Unificada (2026-02-17)

- [X] Link "Requisições e OS" no menu lateral (visível quando ALMOXARIFADO ou ORDENS_SERVICO)
- [X] Suporte a `modulos?: ModuloSistema[]` no MenuLink para link visível quando QUALQUER módulo ativo
- [X] Cards aprimorados: empresa em destaque, contrato em azul, barra progresso (laranja >70%, vermelho >90%), data aprovação em evidência
- [X] Requisições: badge Atendida Sim/Não, aprovador
- [X] OS: consumido, saldo, % utilizado com cores

## Tarefa Concluída: Fluxo de OS Configurável pelo Admin (2026-02-24)

- [X] Backend: Campo fluxo_os (REQUISICAO | MODULO_OS) na entity Orgao + migration
- [X] Backend: PUT /orgaos/:id/modulos aceita fluxo_os; GET /orgaos e /me retornam fluxo_os
- [X] Backend: MedicaoService getOSAtiva/listarOS dual-source (Requisicao ou OrdemServicoContrato conforme fluxo)
- [X] Backend: resumoMedicoes inclui fluxo_os no retorno
- [X] Admin: Seletor "Fluxo de OS" no modal de módulos (quando ORDENS_SERVICO ativo)
- [X] TabMedicao: Link dinâmico - REQUISICAO → /requisicoes/nova?contrato=X&tipo=ORDEM_SERVICO; MODULO_OS → /ordens-servico
- Regra: ORDENS_SERVICO desativado → sempre fluxo REQUISICAO

## ✅ Tarefa Concluída: Combobox Fornecedor com Busca em Tempo Real (2026-02-17)

- [X] Novo Contrato: Popover + Command (cmdk) — digitar filtra fornecedores em tempo real
- [X] Editar Contrato: mesmo combobox aplicado
- [X] Fechar popover ao selecionar; limpar busca ao fechar
- [X] Botão "Novo" mantido para cadastro rápido

## ✅ Tarefa Concluída: Logo do Órgão (2026-02-20)

- [X] Backend: campo logo_url em Orgao + migration 1768400004000-AddLogoOrgao
- [X] Backend: POST /api/orgaos/:id/logo com FileInterceptor (PNG/JPG, 2MB)
- [X] Backend: OrgaosService.uploadLogo, remove logo anterior ao substituir
- [X] Frontend: botão Enviar Logo funcional em Configurações (input file hidden)
- [X] Frontend: preview da logo na configurações, carregar orgao de /api/orgaos/me
- [X] Frontend: logo no Sidebar (área órgão) e no Header
- [X] Backend: logo nos PDFs (OS e Medição) via GeradorPdfService.escreverCabecalho
- [X] Requisicao findOne: adicionada relation 'orgao' para PDF

## ✅ Tarefa Concluída: Opção Enviar ao Fornecedor na Aprovação (2026-02-20)

- [X] Backend: validarOrgaoRequisicao em RequisicaoService (usado pelo endpoint enviar-ao-fornecedor)
- [X] Modal autorizar: checkbox "Enviar notificação ao fornecedor agora" (default: marcado)
- [X] handleAutorizar: inclui enviar_ao_fornecedor no body conforme checkbox
- [X] Mensagem de sucesso diferenciada quando usuário não envia (pode enviar depois)
- [X] Botão "Enviar ao fornecedor" (ícone Send) na coluna Ações para OS aprovadas com PDF
- [X] Modal Enviar/Reenviar ao fornecedor com email/telefone editáveis
- [X] POST /api/almoxarifado/requisicoes/:id/enviar-ao-fornecedor já existia

## ✅ Tarefa Concluída: OS Assinada + Notificação ao Fornecedor (2026-02-20)

- [X] Remover download automático do PDF no frontend
- [X] TipoNotificacao ORDEM_SERVICO_APROVADA
- [X] Backend: ao aprovar OS, envia email (com PDF), notificação no sistema e WhatsApp ao fornecedor
- [X] Link da notificação: /fornecedor/contratos/${contratoId} (rota existente)
- [X] Fornecedor vê notificações via listarPorUsuarioSemOrgao(user.sub) onde user.sub = fornecedor.id
- [X] Build backend OK
- [X] Correção codigo_validacao varchar(16) - armazenar sem hífens
- [X] Validação documento: rota alias, decodeURIComponent, getApiBase()
- [X] Modal autorizar: email e telefone editáveis antes de enviar
- [X] Retorno notificacoes_fornecedor com resultado de cada envio (email, notificacao, whatsapp)
- [X] DevContext: docs/DEVCONTEXT-OS-NOTIFICACOES.md

## ✅ Tarefa Concluída: Configuração de Email (SMTP/IMAP) por Órgão (2026-02-18)

Implementado conforme plano anexo:

**Fase 1 (SMTP) - já estava feita:**
- Migration + campos Orgao (SMTP)
- EmailModule + EmailService
- Endpoints e página Admin de config SMTP
- Integração NotificacoesService e OrdemFornecimentoService

**Fase 2 (IMAP) - implementada agora:**
- [X] Campos IMAP na entidade Orgao: email_imap_host, email_imap_port, email_imap_user, email_imap_senha
- [X] Migration 1768400001000-AddEmailImapOrgao.ts
- [X] ImapModule + ImapService (imapflow)
- [X] OrgaosService: getEmailConfig e atualizarEmailConfig com campos IMAP
- [X] Endpoints: GET /orgaos/:id/emails, GET /orgaos/:id/emails/:uid, POST /orgaos/:id/emails/:uid/responder, POST /orgaos/:id/email-config/testar-imap
- [X] Admin modal: abas SMTP e IMAP, botão Testar IMAP
- [X] Página /orgao/emails (caixa de entrada, listar, ver detalhe, responder)
- [X] Link "Caixa de Entrada" no sidebar do órgão

Arquivos criados/modificados:
- backend/src/imap/imap.service.ts, imap.module.ts
- backend/src/orgaos/entities/orgao.entity.ts
- backend/src/orgaos/orgaos.service.ts, orgaos.controller.ts, orgaos.module.ts
- backend/src/migrations/1768400001000-AddEmailImapOrgao.ts
- frontend/src/app/admin/orgaos/page.tsx
- frontend/src/app/orgao/emails/page.tsx
- frontend/src/components/layout/navigation.tsx

## ✅ Tarefa Concluída: Ações Rápidas na Gestão de Contratos (2026-02-17)

Implementado conforme plano:
- [X] Verificar backend: GET /api/contratos retorna modalidade_execucao, total_itens, itens
- [X] DropdownMenu "Mais ações" na coluna Ações (só aparece quando há ações)
- [X] Modal Solicitar medição (mês referência + mensagem) → POST /api/contratos/:id/medicoes/solicitar
- [X] Links: Ver medições (?tab=medicao), Nova requisição (?contrato=id), Criar ordem de serviço (?tab=ordens-servico)
- [X] Regras condicionais: MEDICAO+VIGENTE (solicitar), MEDICAO (ver), Almoxarifado+itens+VIGENTE (nova req), ORDEM_SERVICO+VIGENTE (criar OS)
- [X] Página de detalhe do contrato aceita ?tab= para abrir aba específica

Arquivos modificados: frontend/src/app/orgao/contratos/page.tsx, frontend/src/app/orgao/contratos/[id]/page.tsx

## ✅ Tarefa Concluída: Sistema de Módulos Opcionais por Órgão - COMPLETO E TESTADO

### Resumo da Implementação (Atualizado 2026-01-19)
Sistema completo de ativação/desativação de módulos por órgão implementado e testado em produção.

**Backend:**
- ✅ Enum ModuloSistema com 10 módulos (todos opcionais, nenhum obrigatório)
- ✅ Campo modulos_habilitados na entidade Orgao (tipo simple-array)
- ✅ Endpoints: GET /api/orgaos/modulos/disponiveis, GET /api/orgaos/:id/modulos, PUT /api/orgaos/:id/modulos
- ✅ Endpoint GET /api/orgaos/me - Retorna órgão logado com módulos atualizados do banco
- ✅ Decorator @RequireModule e guard ModuloGuard registrado como global
- ✅ Guard aplicado em: ContratosController, ContratacaoDiretaController, LicitacoesController, DemandasController, PcaController, PncpController, AtasController
- ✅ ModuloGuard carrega orgao do banco e valida permissão antes de permitir acesso

**Frontend:**
- ✅ Hook useModulosOrgao SEMPRE busca módulos da API (fonte da verdade)
- ✅ Componente ModuleGuard protege páginas de módulos no frontend
- ✅ Menu Sidebar filtra links baseado nos módulos habilitados
- ✅ Página /admin/modulos para gerenciar módulos dos órgãos
- ✅ Acesso direto via URL é bloqueado e redireciona para Dashboard

**Geração Automática de Contratos:**
- ✅ Método gerarContratoAutomatico() no ContratosService
- ✅ Integrado ao método homologar() do LicitacoesService
- ✅ Gera contrato automaticamente após homologação da licitação

**Correções Importantes (2026-01-19):**
- ✅ Corrigido hook useModulosOrgao que detectava admin_token erroneamente
- ✅ Criado componente ModuleGuard para proteção de rotas no frontend
- ✅ Adicionado campo orgao_id na entidade PncpSync
- ✅ Corrigido listarPendentes/listarErros no PncpService (QueryBuilder com left join)

### Arquivos Principais do Sistema de Módulos
- backend/src/orgaos/enums/modulos.enum.ts - Enum e descrições dos módulos
- backend/src/orgaos/entities/orgao.entity.ts - Campo modulos_habilitados
- backend/src/auth/modulo.guard.ts - Guard de verificação de módulos
- backend/src/auth/require-module.decorator.ts - Decorator @RequireModule
- frontend/src/hooks/useModulosOrgao.ts - Hook de busca de módulos
- frontend/src/components/ModuleGuard.tsx - Componente de proteção de páginas
- frontend/src/components/layout/navigation.tsx - Menu com filtro de módulos

### Decisões Técnicas Importantes
- **localStorage**: Usado APENAS para tokens (access_token, orgao_token, admin_token)
- **Módulos**: SEMPRE vêm do banco de dados (fonte da verdade via /api/orgaos/me)
- **Proteção dupla**: Backend (ModuloGuard) + Frontend (ModuleGuard componente)
- **authFetch vs adminFetch**: Páginas /admin usam adminFetch(), páginas /orgao usam authFetch()

### Próximas Tarefas Sugeridas
1. Aplicar @RequireModule em mais controllers conforme necessário
2. Testar geração automática de contratos após homologação
3. Implementar página de logs de sincronização PNCP por órgão

## Tarefa Anterior: Implementar Sistema de Autenticação JWT

### Objetivo
Implementar sistema completo de autenticação JWT para proteger todas as APIs do backend. Atualmente TODAS as 228 rotas de escrita (POST/PUT/PATCH/DELETE) estão completamente abertas sem nenhuma proteção.

### Plano de Implementação

[ ] 1. Instalar dependências necessárias (@nestjs/jwt, @nestjs/passport, passport, passport-jwt)
[ ] 2. Criar estrutura de autenticação:
    [ ] auth.service.ts - Serviço de autenticação com geração/validação de JWT
    [ ] jwt.strategy.ts - Estratégia Passport JWT
    [ ] jwt-auth.guard.ts - Guard para proteger rotas
    [ ] public.decorator.ts - Decorator para marcar rotas públicas
    [ ] roles.decorator.ts - Decorator para RBAC
    [ ] roles.guard.ts - Guard para verificar roles
[ ] 3. Atualizar módulo auth.module.ts com configurações JWT
[ ] 4. Atualizar logins existentes para retornar JWT:
    [ ] orgaos.controller.ts - Login de órgão
    [ ] fornecedores.controller.ts - Login de fornecedor
[ ] 5. Criar middleware de validação de órgão (verificar se usuário pertence ao órgão)
[X] 6. Proteger TODAS as rotas de escrita com guard global (APP_GUARD)
[X] 7. Manter rotas públicas:
    - POST /api/orgaos/login ✅
    - POST /api/fornecedores/login ✅
    - GET /api/licitacoes (consulta pública) ✅
    - GET /api/licitacoes/:id ✅
    - GET /api/licitacoes/publicas ✅
    - GET /api/documentos/licitacao/:id/publicos ✅
    - GET /api/documentos/publicos/lista ✅
    - GET /api/documentos/publicos/:id/download ✅
    - GET /api/health ✅

### Controllers a proteger (228 endpoints):
- pncp.controller.ts (41 endpoints)
- fornecedores.controller.ts (18 endpoints)
- pca.controller.ts (15 endpoints)
- licitacoes.controller.ts (14 endpoints)
- sessao.controller.ts (14 endpoints)
- demandas.controller.ts (13 endpoints)
- itens.controller.ts (11 endpoints)
- E mais 19 controllers

### Stack Tecnológica
- NestJS 11
- TypeORM
- PostgreSQL
- JWT (a implementar)

### Decisões Técnicas Importantes
- Arquitetura de Segurança: Frontend NUNCA decide permissão, Backend SEMPRE valida identidade e escopo via JWT
- Sistema multiusuário/multiorgão: TODAS as consultas de dados devem filtrar por orgao_id do usuário logado
- NUNCA usar atalhos de desenvolvimento - sempre pensar em segurança

# Lessons

## User Specified Lessons

- Sempre fazer git commit e push após alterações no código.
- You have a python venv in ./venv. Use it.
- Include info useful for debugging in the program output.
- Read the file before you try to edit it.
- Due to Cursor's limit, when you use `git` and `gh` and need to submit a multiline commit message, first write the message in a file, and then use `git commit -F <filename>` or similar command to commit. And then remove the file. Include "[Cursor] " in the commit message and PR title.
- **SEMPRE consultar o DevContext (mcp_devcontext_get_project_context) ANTES de iniciar qualquer implementação**: Verificar notas da IA, features existentes, decisões técnicas e histórico de problemas identificados. Isso evita retrabalho e garante alinhamento com decisões já tomadas.

## Cursor learned

- Config email: SMTP = envio (smtp.gmail.com:587), POP/IMAP = recepção (pop/imap.gmail.com:993,995). Erro "Invalid greeting" + "Gpop" = usuário usou servidor POP na aba SMTP
- For search results, ensure proper handling of different character encodings (UTF-8) for international queries
- Add debug information to stderr while keeping the main output clean in stdout for better pipeline integration
- When using seaborn styles in matplotlib, use 'seaborn-v0_8' instead of 'seaborn' as the style name due to recent seaborn version changes
- Use 'gpt-4o' as the model name for OpenAI's GPT-4 with vision capabilities

